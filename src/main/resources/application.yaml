spring:
  profiles:
    active: local


---
spring:
  application:
    name: admin-sample-local
  config:
    activate:
      on-profile: local

---
spring:
  application:
    name: admin-sample-dev
  config:
    activate:
      on-profile: dev

---
spring:
  application:
    name: admin-sample-prod
  config:
    activate:
      on-profile: prod

---
jwt:
  header: Authorization
  #echo 'ssac-msacloud-1-cloudlibrary-jwt-secretkey-base64'|base64
  secret:c3NhYy1tc2FjbG91ZC0xLWNsb3VkbGlicmFyeS1qd3Qtc2VjcmV0a2V5LWJhc2U2NA==
  token-validity-in-seconds: 86400